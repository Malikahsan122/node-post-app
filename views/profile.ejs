<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-800 px-10 text-white">
      <div class="pt-8 flex justify-end">
        <a
          class="px-2 py-1 bg-blue-400 rounded-xl border-1 border-blue-300"
          href="/logout"
          >Logout</a
        >
      </div>
      <h2 class="mb-6">hey <%=user.username%> Welcome Create your posts</h2>
      <form id="postForm">
        <textarea
          class="w-1/3 resize-none bg-transparent outline-none border-1 border-zinc-700 px-2 py-1"
          name="content"
          id="content"
        >
<%= editPost ? editPost.content : '' %></textarea
        >
        <input
          type="hidden"
          id="editing"
          value="<%= editPost ? 'true' : 'false' %>"
        />
        <input
          type="hidden"
          id="editId"
          value="<%= editPost ? editPost._id : '' %>"
        />
        <input
          class="block bg-blue-500 rounded-xl px-2 py-1"
          type="submit"
          value="<%= editPost ? 'Update Post' : 'Create Post' %>"
        />
      </form>
      <div class="post mt-10">
        <%user.posts.forEach((post)=>{%>
        <div class="posts bg-transparent mt-5 w-1/3 border-1 px-2 py-1">
          <h1 class="text-blue-500"><%=user.username%></h1>
          <p><%=post.content%></p>
          <span class="block"><%=post.likes.length%> likes</span>
          <div class="flex justify-between">
            <div>
              <a class="inline-block text-blue-600" href="/like/<%=post._id%>">
                <%= post.likes.indexOf(user._id)===-1?"like":"unlike"%>
              </a>
            </div>
            <div>
              <a
                class="inline-block bg-blue-400 rounded-xl px-2 py-1"
                href="/edit/<%= post._id%>"
                >Update</a
              >
              <a
                class="inline-block bg-blue-400 rounded-xl px-2 py-1"
                href="/delete"
                >Delete</a
              >
            </div>
          </div>
        </div>
        <%})%>
      </div>
    </div>
  </body>
  <script src="/js/index.js"></script>
</html>
